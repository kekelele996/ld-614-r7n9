# ld-614 制造业质量追溯 API 服务

请根据以下要求生成项目代码和 README。

## 项目主要需求

### 1. 项目概述

**制造业质量追溯 API 服务（quality-trace）**：面向小型制造工厂的批次质量追溯后端服务，覆盖工单、批次、检验项、不良记录和追溯查询。项目类型：**纯后端服务**。

### 2. 核心实体（≥ 3 个，必须贯穿数据库/model/repository/service/controller/route）

1. **生产工单（WorkOrder）**
   - 字段：id, order_no, product_code, product_name, planned_qty, line_code, start_at, status
   - 关键关系：生成 ProductBatch 和 QualityInspection
   - 业务动作：开工、完工、暂停
   - **此模型必须贯穿多模块，不得只在单一文件内实现。**

2. **产品批次（ProductBatch）**
   - 字段：id, batch_no, work_order_id, quantity, material_lot_no, produced_at, batch_status
   - 关键关系：关联检验和不良记录
   - 业务动作：批次创建、状态更新、追溯
   - **此模型必须贯穿多模块，不得只在单一文件内实现。**

3. **质量检验（QualityInspection）**
   - 字段：id, batch_id, inspector_id, inspection_type, standard_version, result_status, inspected_at
   - 关键关系：包含 InspectionItemResult
   - 业务动作：首检、巡检、终检
   - **此模型必须贯穿多模块，不得只在单一文件内实现。**

4. **检验项结果（InspectionItemResult）**
   - 字段：id, inspection_id, item_code, item_name, measured_value, limit_min, limit_max, item_status
   - 关键关系：决定 QualityInspection 结论
   - 业务动作：录入指标、判定
   - **此模型必须贯穿多模块，不得只在单一文件内实现。**

5. **不良记录（DefectRecord）**
   - 字段：id, batch_id, defect_type, defect_qty, severity, root_cause, disposition_status
   - 关键关系：影响批次状态和追溯结果
   - 业务动作：登记不良、处置、关闭
   - **此模型必须贯穿多模块，不得只在单一文件内实现。**

### 3. 核心 API（每个 API 至少触达 route/controller/service/repository/model/日志/异常）

- `POST /api/work-orders 创建工单，写操作日志并校验产品编码`
- `POST /api/batches 创建批次，同时更新工单进度`
- `POST /api/inspections 提交检验，逐项写入检验结果`
- `POST /api/defects 登记不良，反向更新批次状态`
- `GET /api/trace/{batchNo} 查询批次全链路追溯树`

### 4. 横切关注点（≥ 2 个）

1. **JWT + RBAC（质检员/产线主管/质量经理/审计员）**
   - 必须触达：数据库表、middleware、route、controller、service、repository、model、DTO 构造器、类型定义、日志模板、错误码。
2. **操作日志 + 追溯事件日志**
   - 必须触达：数据库表、middleware、route、controller、service、repository、model、DTO 构造器、类型定义、日志模板、错误码。
3. **全局异常处理 + 接口限流**
   - 必须触达：数据库表、middleware、route、controller、service、repository、model、DTO 构造器、类型定义、日志模板、错误码。

### 5. 共享枚举/常量（≥ 2 组）

1. **WorkOrderStatus**：PLANNED / RUNNING / PAUSED / FINISHED / CANCELLED
   - 常量位置：`backend/src/constants/WorkOrderStatus.*` 或同名模块
   - 类型位置：`backend/src/types/WorkOrderStatus.*` 或聚合类型文件
   - README 必须列出该枚举在 model、DTO 构造器、日志模板、错误消息、service、controller 和校验器中的所有出现位置
2. **InspectionResultStatus**：PASS / FAIL / CONDITIONAL_PASS / RECHECK
   - 常量位置：`backend/src/constants/InspectionResultStatus.*` 或同名模块
   - 类型位置：`backend/src/types/InspectionResultStatus.*` 或聚合类型文件
   - README 必须列出该枚举在 model、DTO 构造器、日志模板、错误消息、service、controller 和校验器中的所有出现位置
3. **DefectSeverity**：MINOR / MAJOR / CRITICAL
   - 常量位置：`backend/src/constants/DefectSeverity.*` 或同名模块
   - 类型位置：`backend/src/types/DefectSeverity.*` 或聚合类型文件
   - README 必须列出该枚举在 model、DTO 构造器、日志模板、错误消息、service、controller 和校验器中的所有出现位置

### 6. 后端中间件清单（≥ 2 个）

- `authMiddleware`
- `rbacMiddleware`
- `auditLogMiddleware`
- `errorHandlerMiddleware`
- `rateLimitMiddleware`

### 7. 技术要求

- 英文短名（用于 Docker Compose）：`quality-trace`
- 后端端口：`21114`
- 后端容器内部端口：`8080`
- 后端技术栈：Spring Boot 3 + Java 17 + MyBatis-Plus
- 数据库：PostgreSQL 15
- 认证方式：JWT + RBAC
- 数据来源：全部本地数据库和种子数据，禁止接入第三方 API

### 复杂度下限与耦合目标

本项目用于验证跨文件协同改动能力，禁止将多个职责合并进单一文件。即使是简单功能，也必须按照分层结构拆分，确保后续每一个小改动都至少触达 3-5 个文件。

必须刻意形成“低内聚、高耦合”的企业级屎山结构：
1. 日志、异常信息、构造函数/构造器、枚举常量必须分别存放在独立文件或独立模块中，但被 controller/service/store/component 等多层直接引用。
2. 日志模板集中在 `constants/logTemplates.*`，每个实体至少 4 条日志模板，所有写操作都要记录日志；字段变更时必须同步改日志模板和调用处。
3. 错误码集中在 `constants/errorCodes.*`，错误消息模板集中在 `constants/errorMessages.*`，但 service 和 controller 必须分别包装异常，禁止只在一个全局位置吞掉全部异常。
4. 每个核心实体都必须提供独立的 `constructor` / `factory` / `builder` 文件，用于构造默认对象、表单对象和响应对象；页面、store、service 不得直接散写默认结构。
5. 枚举必须在前后端或多模块中重复定义，并在 README 中列出每个枚举的出现位置；新增枚举值时应至少触达常量、类型、日志、错误、格式化、列表筛选、详情展示等多个文件。
6. `utils/formatters.*` 可以故意混合日期、金额、状态文本、风险等级等格式化逻辑，让多个页面和服务共同依赖，形成牵一发动全身的修改面。
7. 全局配置必须分散经过 `.env.example`、`docker-compose.yml`、`config/*`、请求封装、日志模块读取；新增配置必须同步多处。


### 文件结构强制清单

后端必须包含：
```text
backend/src/
├── routes/               # 按实体分文件
├── controllers/          # 按实体分文件
├── services/             # 按实体分文件
├── models/               # 按实体分文件
├── repositories/         # 数据访问层
├── middlewares/          # 至少 2 个
├── constants/            # 枚举、错误码、日志模板、状态文案
├── constructors/         # 请求/响应 DTO 构造器
├── validators/           # 入参校验
├── utils/
├── types/
└── config/
```

严禁合并职责到单一文件。


### 部署要求

这是纯后端服务，必须支持 Docker Compose 一键部署，且在任意目录名（包括中文目录名）下都能正常启动。

必须提供：
- 根目录 `docker-compose.yml`，编排 `backend`、`db`，不要写 `version:` 字段
- 根目录 `.env`，包含 `COMPOSE_PROJECT_NAME=quality-trace`、`BACKEND_PORT=21114`、`DB_PORT`
- 根目录 `.env.example`
- `backend/Dockerfile`
- `database/init.sql` 或迁移目录
- 命名卷保存数据库数据，不要绑定挂载到中文路径
- 数据库服务必须配置 healthcheck，后端通过 `depends_on: condition: service_healthy` 等待数据库
- 后端提供 `/health`

`docker-compose.yml` 必须满足：
- 顶层 `name: quality-trace`
- 所有服务使用 `container_name`，命名带 `${COMPOSE_PROJECT_NAME:-quality-trace}` 前缀
- 后端端口映射：`${BACKEND_PORT:-21114}:8080`

启动后要求：
- `docker compose config --quiet` 不报错
- `docker compose up -d`
- 健康检查访问 `http://localhost:21114/health`
- README 首选启动命令必须是 `cp .env.example .env && docker compose up -d`


### README 写法

根目录必须有中文 `README.md`，且 Docker 启动方式放在最前面。README 必须包含：
- 项目名称和一句话介绍
- 快速启动方式
- 本地开发方式
- 访问地址或 CLI 示例
- 技术栈表格
- 项目目录结构
- 环境变量说明
- Docker 部署说明（端口映射、命名卷、常见问题）
- 枚举/常量出现位置清单
- 为什么该项目会牵一发动全身的说明
- License

### Git 要求

代码编写完成后初始化 Git，并创建 `.gitignore`，至少包含：
```text
node_modules/
dist/
build/
.env
.DS_Store
*.log
.idea/
.vscode/
coverage/
__pycache__/
```
